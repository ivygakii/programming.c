//program to check if aperon is eligible to vote
#include <stdio.h>
int main() {
int age;
printf("Enter age of the person:");
scanf("%d" ,&age);
if(age<=18);{
printf("you are not eligible to vote:");
else
if(age>=18);
printf("you are eligible to vote:");
return 0;
}